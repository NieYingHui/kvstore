# bench.conf - benchmarking config (minimize persistence impact)

port 2000

# Keep persistence off for CPU/memory allocator comparisons
enable_persistence no
appendonly no
save ""

# Use the simplest network backend for stable results
network_backend reactor
