1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。


1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。


1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。


1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。


1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。


1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。


1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。


1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。


1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。


1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。


1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。


1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。


1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。


1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。


1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。


1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。


1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。


1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。

1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。


1：ntyco需要作为kvstore的submodule，提交到gitlab。   						（√）
2：增量持久化与全量持久化，二选一演示，不需要同时开启。						（√）
3：编译过程不要存在警告。													（√）
4：内存池需要把虚拟内存，物理内存占用情况，在readme里面体现出来。 （一半，有提升）
5：tcp分包与粘包问题。需要在tcp包前面，引入tcp包总长。						（√）	
6：multi-cmd设计有缺陷。不能通过加usleep来解决粘包问题。						（√）
7：README需要包含详细的编译步骤，测试方案与测试方案可行性，性能数据。
8：key与value是需要支持特殊字符，空格回车换行，可以参考redis的resp协议。
9：主从同步调研ebpf，测试主从同步性能差异。
10： 持久化落盘用io_uring；加载数据用mmap；								（√）
11：引入配置文件，把持久化，日志，端口ip，主从同步。